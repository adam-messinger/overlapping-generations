{
  "name": "tsimulation",
  "version": "0.1.0",
  "description": "TypeScript micro-framework for modular simulations",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npx tsx src/simulation.ts",
    "test": "find src -name '*.test.ts' -print | sort | while read -r f; do echo \"Running $f\"; npx tsx \"$f\" || exit 1; done",
    "capture-baseline": "npx tsx scripts/baseline.ts --output=baselines/current.json",
    "regression": "npm run capture-baseline && npx tsx scripts/compare-baselines.ts --strict baselines/reference.json baselines/current.json",
    "bless-baseline": "npx tsx scripts/bless-baseline.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0"
  }
}
